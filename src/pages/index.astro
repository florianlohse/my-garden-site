---
import { getCollection } from 'astro:content';
const allNotes = await getCollection('notes');
---
<html>
  <head><title>My Garden</title></head>
  <body>
    <h1>🌱 My Digital Garden</h1>
    <ul>
      {allNotes.map(note => (
        <li>
          <a href={`/my-garden-site/${note.id}/`}>{note.id}</a>
        </li>
      ))}
    </ul>
  </body>
</html>